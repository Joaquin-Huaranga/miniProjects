<section class="contact-section text-white py-5">
  <div class="container-wrapper">
    <div class="container position-relative text-center px-4 px-md-5">
      <h2 class="fw-bold mb-3 display-6">¿Aún tienes alguna pregunta?</h2>
      <p class="mb-4 fs-6">
        No dudes en dejarnos tu número de teléfono. Nos pondremos en contacto contigo para resolver
        cualquier duda que tengas.
      </p>

      <form
        id="subscribeForm"
        class="subscribe-form d-flex flex-column flex-md-row justify-content-center align-items-stretch gap-2 mx-auto"
        style="max-width: 500px; z-index: 2; position: relative;"
      >
        <input
          type="tel"
          id="phoneInput"
          class="form-control bg-dark border-0 text-white px-3 py-2"
          placeholder="Introduce tu número de teléfono"
          required
          aria-label="Número de teléfono"
          style="border-radius: 10px;"
        />
        <button
          type="submit"
          class="btn btn-warning text-dark fw-semibold px-4 py-2"
          style="border-radius: 10px;"
        >
          Suscríbete
        </button>
      </form>
      <div
        class="modal fade"
        id="successModal"
        tabindex="-1"
        aria-labelledby="successModalLabel"
        aria-hidden="true"
      >
        <div class="modal-dialog modal-dialog-centered">
          <div class="modal-content text-dark">
            <div class="modal-header">
              <h5 class="modal-title" id="successModalLabel">¡Gracias por suscribirte!</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"
              ></button>
            </div>
            <div class="modal-body">
              Nos pondremos en contacto contigo muy pronto. ¡Estás un paso más cerca de descubrir lo
              mejor de Edudu!
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Cerrar</button>
            </div>
          </div>
        </div>
      </div>

      <img src="/images/person.webp" class="circle-img top-start" alt="Persona 1" />
      <img src="/images/person2.webp" class="circle-img top-end" alt="Persona 2" />
      <img src="/images/person3.jpg" class="circle-img bottom-start" alt="Persona 3" />
      <img src="/images/person.webp" class="circle-img bottom-end" alt="Persona 4" />
    </div>
  </div>

  <script>
    document.getElementById('subscribeForm').addEventListener('submit', function (e) {
      e.preventDefault();

      const modal = new bootstrap.Modal(document.getElementById('successModal'));
      modal.show();
      document.getElementById('phoneInput').value = '';
    });
  </script>
</section>

<style>
  .container {
    padding: 5em 2em;
    background-color: #1c1405;
    border-radius: 24px;
    position: relative;
    overflow: hidden;
  }

  .contact-section .form-control:focus {
    box-shadow: none;
  }

  .contact-section .btn-warning {
    background-color: #fec008;
    transition: background-color 0.3s ease;
  }

  .contact-section .btn-warning:hover {
    background-color: #e5ad00;
  }

  .circle-img {
    margin: 2em 2em;
    position: absolute;
    width: 48px;
    height: 48px;
    object-fit: cover;
    border-radius: 50%;
    border: 2px solid white;
    transition: transform 0.3s ease;
    z-index: 1;
  }

  .circle-img:hover {
    transform: scale(1.1);
  }
  .top-start {
    top: 20px;
    left: 20px;
  }

  .top-end {
    top: 20px;
    right: 20px;
  }

  .bottom-start {
    bottom: 20px;
    left: 20px;
  }

  .bottom-end {
    bottom: 20px;
    right: 20px;
  }

  @media (max-width: 768px) {
    .circle-img {
      display: none;
    }

    .contact-section h2 {
      font-size: 1.5rem;
    }

    .contact-section p {
      font-size: 0.95rem;
    }
  }
</style>
