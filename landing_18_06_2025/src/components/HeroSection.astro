<section id="hero" class="hero-section d-flex align-items-center text-dark py-5">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-lg-6 mb-4 mb-lg-0">
        <div class="hero-content">
          <h1 class="display-4 fw-bold">Conexión del Conocimiento</h1>
          <h2 class="h4 mb-4">Abre la Puerta al Futuro</h2>
          <p class="lead">
            Brindamos a cada estudiante la oportunidad de acceder a la mejor educación y abrirle las puertas al mundo del conocimiento.
            <br />
            Comienza hoy tu camino de aprendizaje con <strong>Edudu</strong> y conviértete en un estudiante destacado en nuestra comunidad.
          </p>
          <a href="#" class="btn btn-lg btn-custom mt-3">¡Empieza hoy!</a>
        </div>
      </div>

      <div class="col-lg-6 text-center">
        <img src="images/hero_bg.png" alt="Imagen de héroe" class="img-fluid hero-img" />
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    background: url('/images/bg_texto_hero.png') center center / cover no-repeat;
    min-height: 100vh;
    color: #222;
    position: relative;
  }

  .hero-section .container {
    position: relative;
    z-index: 1;
  }

  .hero-content h1 {
    font-size: 3rem;
    line-height: 1.2;
    color: #222;
    text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.5);
  }

  .hero-content h2 {
    color: #444;
    font-weight: 600;
    text-shadow: 1px 1px 1px rgba(255, 255, 255, 0.4);
  }

  .hero-content p {
    font-size: 1.1rem;
    color: #333;
    text-shadow: 1px 1px 1px rgba(255, 255, 255, 0.3);
  }

  .btn-custom {
    background-color: #FEC458;
    color: #fff;
    padding: 0.75rem 1.5rem;
    font-weight: 600;
    border-radius: 0.5rem;
    border: none;
    transition: background-color 0.3s ease;
  }

  .btn-custom:hover {
    background-color: #FFB640;
    color: #fff;
  }

  .hero-img {
    max-width: 100%;
    height: auto;
  }
</style>
