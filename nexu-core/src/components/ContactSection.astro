---
import { Icon } from 'astro-icon/components';
---
<section
  id="contacto"
  class="relative bg-gradient-to-b from-black via-gray-900 to-gray-950 text-gray-100 py-24
         overflow-x-clip  <!-- corta cualquier desbordamiento lateral en XS -->"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <header class="text-center mb-16">
      <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight">
        <span class="bg-gradient-to-r from-blue-400 to-cyan-500 bg-clip-text text-transparent">Contáctanos</span>
      </h2>
      <p class="mt-4 text-gray-400 max-w-2xl mx-auto">
        ¿Tienes preguntas o deseas una cotización personalizada? Completa el formulario o escríbenos directamente.
      </p>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-start">

      <div class="space-y-8">
        <div class="flex items-start gap-4">
          <Icon name="mdi:map-marker" width="26" class="text-blue-500 mt-1" />
          <div>
            <h4 class="text-lg font-semibold">Dirección</h4>
            <p class="text-gray-400">Av. Nexucore 123, San Juan de Miraflores, Lima – Perú</p>
          </div>
        </div>

        <div class="flex items-start gap-4">
          <Icon name="mdi:email-outline" width="26" class="text-blue-500 mt-1" />
          <div>
            <h4 class="text-lg font-semibold">Correo electrónico</h4>
            <p class="text-gray-400">soporte@nexucore.pe</p>
          </div>
        </div>

        <div class="flex items-start gap-4">
          <Icon name="mdi:phone" width="26" class="text-blue-500 mt-1" />
          <div>
            <h4 class="text-lg font-semibold">Teléfono</h4>
            <p class="text-gray-400">+51 987 654 321</p>
          </div>
        </div>

        <div class="flex gap-4 pt-4 text-[4em]">
          <a href="#" aria-label="Facebook" class="hover:text-blue-400 transition">
            <Icon name="mdi:facebook" width="24" />
          </a>
          <a href="#" aria-label="Instagram" class="hover:text-blue-400 transition">
            <Icon name="mdi:instagram" width="24" />
          </a>
          <a href="#" aria-label="WhatsApp" class="hover:text-blue-400 transition">
            <Icon name="mdi:whatsapp" width="24" />
          </a>
        </div>
      </div>

      <form
        id="contact-form"
        action="#"
        class="bg-black/60 backdrop-blur-md border border-gray-800 rounded-2xl p-8 space-y-6"
      >
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          <input type="text" name="nombre" required placeholder="Nombre completo" class="w-full px-4 py-3 bg-gray-900 text-white border border-gray-700
           rounded-md placeholder-gray-500 focus:outline-none focus:ring-2
           focus:ring-blue-500 transition;" />
          <input type="email" name="email" required placeholder="Correo electrónico" class="w-full px-4 py-3 bg-gray-900 text-white border border-gray-700
           rounded-md placeholder-gray-500 focus:outline-none focus:ring-2
           focus:ring-blue-500 transition;" />
        </div>

        <input type="text" name="asunto" required placeholder="Asunto" class="w-full px-4 py-3 bg-gray-900 text-white border border-gray-700
           rounded-md placeholder-gray-500 focus:outline-none focus:ring-2
           focus:ring-blue-500 transition;" />

        <textarea name="mensaje" required placeholder="Escribe tu mensaje…" rows="5"
                  class="w-full px-4 py-3 bg-gray-900 text-white border border-gray-700
           rounded-md placeholder-gray-500 focus:outline-none focus:ring-2
           focus:ring-blue-500 transition; resize-none"></textarea>

        <button
          type="submit"
          class="w-full inline-flex justify-center items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700
                 text-white font-semibold rounded-md transition"
        >
          <Icon name="mdi:send" width="20" />
          Enviar mensaje
        </button>

        <p id="form-success" class="hidden text-center text-sm text-green-400 font-medium">
          ¡Gracias! Hemos recibido tu mensaje.
        </p>
      </form>
    </div>
  </div>

  <div
    class="pointer-events-none absolute top-0 left-1/2 -translate-x-1/2
           w-[600px] h-[600px] md:w-[800px] md:h-[800px]
           bg-blue-500/10 blur-[180px] rounded-full -z-10"
  ></div>
</section>


<script type="module">
  const form = document.getElementById('contact-form');
  const ok   = document.getElementById('form-success');
  form?.addEventListener('submit', (e) => {
    e.preventDefault();        // evita que la página se recargue
    ok?.classList.remove('hidden');
    form.reset();              // limpia los campos
  });
</script>
