---
import { Icon } from 'astro-icon/components';
const offers = [
  {
    title: 'RTX 4070 SUPER',
    description: '12 GB GDDR6X | Triple‑fan RGB',
    image: '/images/offers-images/offer-1.jpg',
    icon: 'mdi:television',
    oldPrice: 899,
    newPrice: 749,
    slug: 'rtx-4070-super'
  },
  {
    title: 'Kit DDR5 32GB 6000 MHz',
    description: 'CL30| Heat‑spreader negro mate',
    image: '/images/offers-images/offer-2.jpg',
    icon: 'mdi:memory',
    oldPrice: 259,
    newPrice: 199,
    slug: 'ddr5-32gb-6000'
  },
  {
    title: 'SSD NVMe Gen4 2TB',
    description: 'Read 7400MB/s | Heatsink integrado',
    image: '/images/offers-images/offer-3.jpg',
    icon: 'mdi:harddisk',
    oldPrice: 219,
    newPrice: 169,
    slug: 'nvme-2tb-gen4'
  },
  {
    title: 'Monitor 27″ QHD 165Hz',
    description: 'PanelIPS | 1ms GTG | FreeSync Premium',
    image: '/images/offers-images/offer-4.jpg',
    icon: 'mdi:monitor',
    oldPrice: 379,
    newPrice: 299,
    slug: 'monitor-27-qhd-165'
  }
];
---

<section id="ofertas" class="relative bg-gradient-to-b from-gray-950 via-gray-900 to-black text-gray-200 py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <header class="text-center mb-16">
      <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight">
        <span class="bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent">Ofertas&nbsp;Flash</span>
      </h2>
      <p class="mt-4 text-gray-400 max-w-2xl mx-auto">
        Descuentos limitados en hardware de alto rendimiento. ¡Actualiza tu rig antes de que se agoten!
      </p>
    </header>

    <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
      {offers.map((item) => (
        <a
          href={`/ofertas/${item.slug}`}
          class="group relative flex flex-col rounded-2xl overflow-hidden bg-black/70 backdrop-blur-lg border border-gray-800 hover:border-blue-500 transition"
        >
          <img
            src={item.image}
            alt={item.title}
            class="w-full h-56 object-cover opacity-60 group-hover:opacity-40 transition"
            loading="lazy"
          />

          <div class="absolute top-4 right-4 bg-blue-600 text-white text-xs font-bold px-2 py-1 rounded-md shadow-md">
            -
            {Math.round(100 - (item.newPrice / item.oldPrice) * 100)}%
          </div>

          <div class="flex-1 flex flex-col justify-between p-6">
            <div>
              <Icon name={item.icon} width="28" class="text-blue-500 mb-2" />
              <h3 class="text-lg font-semibold">{item.title}</h3>
              <p class="text-sm text-gray-400">{item.description}</p>
            </div>

            <div class="mt-6">
              <div class="text-sm text-gray-400 line-through">${item.oldPrice}</div>
              <div class="text-xl font-bold text-blue-400">${item.newPrice}</div>
            </div>
          </div>

          <div
            class="absolute inset-0 flex items-center justify-center bg-black/70 opacity-0 group-hover:opacity-100 transition"
          >
            <span class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-md text-sm font-semibold">
              <Icon name="mdi:cart-outline" width="18" /> Añadir al carrito
            </span>
          </div>
        </a>
      ))}
    </div>
  </div>

  <div class="pointer-events-none absolute -top-32 left-1/2 -translate-x-1/2 w-[900px] h-[900px] bg-blue-500/10 blur-[200px] rounded-full"></div>
</section>
