import React from 'react';
import {PresentationSection} from "./PresentationSection.jsx";

export const Home = () => {
    return (
        <div>

            <PresentationSection/>
        </div>
    );
};