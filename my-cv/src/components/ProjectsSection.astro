---
import ContentWidth from "./ContentWidth.astro";

const projects = [
    {
        title: "Portafolio Personal",
        image: "/image/temporal-image1.jpg",
        description:
            "Diseñado y desarrollado con Astro y Tailwind, implementando diseño responsivo y buenas prácticas de desarrollo frontend.",
        tech: ["Astro", "Tailwind CSS"],
        link: "/"
    },
    {
        title: "Pagina Web para Circulo Militar - STS",
        image: "/image/temporal-image1.jpg",
        description:
            "Pagina Web para CMSTS usando Astro, Wordpress Headless y Tailwind. Optimizada para móviles.",
        tech: ["Wordpress Headless", "Astro", "Tailwind CSS"],
        link: "https://www.circulomilitar-sts.com/"
    }
];
---

<section id="projects" class="bg-gray-900 py-20">
    <ContentWidth>
        <h2 class="text-4xl font-extrabold text-blue-400 mb-12 text-center">💼 Mis Proyectos</h2>

        <div class="grid gap-10 md:grid-cols-2 lg:grid-cols-3 px-6">
            {projects.map((project) => (
                    <div class="bg-gray-800 rounded-2xl overflow-hidden shadow-md hover:shadow-xl transition duration-300">
                        <img
                                src={project.image}
                                alt={`Imagen de ${project.title}`}
                                class="w-full h-48 object-cover"
                        />
                        <div class="p-6">
                            <h3 class="text-xl font-semibold text-white mb-2">{project.title}</h3>
                            <p class="text-gray-300 text-sm mb-4">{project.description}</p>
                            <div class="flex flex-wrap gap-2 mb-4">
                                {project.tech.map((tech) => (
                                        <span class="bg-blue-950 text-blue-300 text-xs px-3 py-1 rounded-full">{tech}</span>
                                ))}
                            </div>
                            <a
                                    href={project.link}
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    class="inline-block px-4 py-2 bg-blue-600 text-white text-sm rounded-lg hover:bg-blue-700 transition"
                            >
                                Ver Proyecto
                            </a>
                        </div>
                    </div>
            ))}
        </div>
    </ContentWidth>
</section>
